name: CI

on:
  push:
    branches: [main, develop]
  pull_request:
    branches: [main]

jobs:
  backend:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      # 后端成员后续补充具体步骤

  frontend:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      # 前端成员后续补充具体步骤