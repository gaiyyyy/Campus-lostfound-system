## 后端设计贡献说明

姓名：孔欣然

学号：2312190201

日期：2026-4-13

## 个人贡献说明

### 一、完成的工作内容

#### 1. API 实现

- [x] 用户认证 API（注册 / 登录）- 使用 JWT 实现
- [x] 失物信息管理（LostItemController）
  - 发布失物信息
  - 查看失物列表
  - 查看失物详情
  - 编辑失物信息
  - 删除失物信息
  - 更新失物状态（已找回/未找回）
- [x] 招领信息管理（FoundItemController）
  - 发布招领信息
  - 查看招领列表
  - 查看招领详情
  - 编辑招领信息
  - 删除招领信息
  - 更新招领状态（已认领/未认领）
- [x] 用户个人中心管理（ProfileController）
  - 修改用户名
  - 修改密码
  - 查看个人信息
- [x] 管理员功能（AdminController）
  - 用户管理
  - 平台所有失物/招领信息管理
- [x] 统一错误响应格式

#### 2. 数据库

- [x] 建表 SQL- 3 张核心表
  - 用户表（user）
  - 失物信息表（lost_item）
  - 招领信息表（found_item）

- [x] 数据模型定义（Entity 层）
  - 用户表（User）
  - 失物信息表（LostItem）
  - 招领信息表（FoundItem）
- [x] Repository 数据访问层
  - UserRepository
  - LostItemRepository
  - FoundItemRepository

#### 3. Docker 部署

- [x] Dockerfile 编写（基于 openjdk:17-jdk-slim）
- [x] docker-compose.yml 配置（后端 + MySQL）

#### 4. 其他

- [x] JWT 鉴权机制实现（JwtUtils + JwtFilter）
- [x] 配置 CORS 跨域，支持前端对接
- [x] 统一异常处理与响应格式

### 二、遇到的问题及解决方案

#### **问题1：**Docker无法运行

 Windows 虚拟化问题导致 Docker 无法正常运行

#### **解决：** 

1. 尝试开启 BIOS 虚拟化 + 安装 Docker Desktop
2. 尝试安装 Docker Toolbox（bash.exe 缺失）
3. 最终采用本地直接运行方案：Spring Boot + MySQL 本地运行
4. 已确保 API 功能完整，前端可通过 localhost:8080 正常对接

#### 问题 2：前后端跨域问题

前端 Vue 项目访问后端 API 时遇到跨域拦截。

#### **解决：**

在 SecurityConfig 中配置 CORS：

```java
@Bean
public CorsFilter corsFilter() {
    UrlBasedCorsConfigurationSource source = new UrlBasedCorsConfigurationSource();
    CorsConfiguration config = new CorsConfiguration();
    config.setAllowCredentials(true);
    config.addAllowedOriginPattern("*");
    config.addAllowedHeader("*");
    config.addAllowedMethod("*");
    source.registerCorsConfiguration("/**", config);
    return new CorsFilter(source);
}
```

