# 安全审查贡献说明

姓名 ：孔欣然 

学号 ： 2312190201

日期 ： 2026-5-6

## 一、我完成的工作

1. ✅ AI 辅助安全审查（OWASP Top 10 视角）
2. ✅ 修复 3 个安全漏洞（SQL LIKE 注入、信息泄露、IDOR 越权）
3. ✅ 完成安全检查清单（逐项评估并处理）
4. ✅ 配置 CI 密钥泄露扫描（Gitleaks + GitHub Actions）
5. ✅ 创建 `.env.example` 配置模板
6. ✅ 配置 `.gitignore` 忽略敏感文件
7. ✅ 编写安全审查文档 `docs/security-review.md`

## 二、AI 安全审查

### 审查的文件/模块

| 模块 | 文件 |
|------|------|
| 安全配置 | SecurityConfig.java, JwtFilter.java |
| 认证授权 | JwtUtils.java, UserController.java |
| 业务逻辑 | LostItemController.java, LostItemService.java |
| 业务逻辑 | FoundItemController.java, FoundItemService.java |
| 数据访问 | FoundItemRepository.java |
| 管理员功能 | AdminController.java |

### AI 发现的主要问题

| 问题 | 文件 |
|------|------|
| JWT 密钥硬编码 | JwtUtils.java |
| SQL LIKE 通配符注入 | FoundItemRepository.java |
| 错误信息暴露内部细节 | 多个 Controller |
| IDOR 越权漏洞 | LostItemService.java, FoundItemService.java |

### 我修复了哪些问题

| 问题 | 修复方式 |
|------|----------|
| SQL LIKE 注入 | 添加 `JpaUtils.escapeLikeKeyword()` 转义方法，Repository 使用 `ESCAPE '\\'` |
| 错误信息暴露 | 新增 `GlobalExceptionHandler` 统一异常处理，敏感信息转通用信息 |
| IDOR 越权 | Service 层所有修改/删除操作添加 `userId` 所有权验证 |

---

## 三、安全检查清单

### 认证与授权

| 检查项                           | 状态     | 说明/处理方式                                                |
| -------------------------------- | -------- | ------------------------------------------------------------ |
| 密码存储使用 bcrypt/argon2 哈希  | ✅ 已满足 | `SecurityConfig.java` 中配置了 `BCryptPasswordEncoder`，注册时调用 `passwordEncoder.encode()` |
| JWT/Session 有过期时间           | ✅ 已满足 | `JwtUtils.java` 中设置 `expiration = 3600000`（1小时），`isExpired()` 方法检查过期 |
| 需要登录的接口都有权限校验       | ✅ 已满足 | `SecurityConfig.java` 配置了 `/api/register`、`/api/login` 放行，其他接口需要认证 |
| 用户只能操作自己的数据（防越权） | ✅ 已修复 | `LostItemService` 和 `FoundItemService` 中已添加所有权验证   |

### 注入防护

| 检查项                                  | 状态     | 说明/处理方式                                                |
| --------------------------------------- | -------- | ------------------------------------------------------------ |
| 使用 ORM 或参数化查询，无字符串拼接 SQL | ✅ 已满足 | 使用 Spring Data JPA，所有查询均为参数化绑定                 |
| LIKE 查询转义特殊字符                   | ✅ 已修复 | 添加 `JpaUtils.escapeLikeKeyword()` 方法转义 `%` 和 `_`，查询使用 `ESCAPE '\\'` |

### 敏感信息

| 检查项                                            | 状态     | 说明/处理方式                  |
| ------------------------------------------------- | -------- | ------------------------------ |
| `.env` 文件已加入 `.gitignore`，有 `.env.example` | ✅ 已修复 | 已创建 `.env.example` 模板文件 |

## 四、CI 安全扫描

### 配置选项

**Gitleaks 密钥泄露扫描** 

### 配置文件

`.github/workflows/security.yml`

```yaml
name: Security Scan

on:
  push:
    branches: [main, master]
  pull_request:
    branches: [main, master]

jobs:
  gitleaks:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
        with:
          fetch-depth: 0
      - uses: gitleaks/gitleaks-action@v2
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
```

### 扫描结果

| 项目          | 结果 |
| ------------- | ---- |
| Workflow 运行 | 成功 |

## 五、PR 链接

[密钥泄露扫描 by gaiyyyy · Pull Request #34 · gaiyyyy/Campus-lostfound-system](https://github.com/gaiyyyy/Campus-lostfound-system/pull/34)

## 六、心得体会

### Vibe Coding 场景下，如何平衡开发效率和安全？

1. **效率优先，安全并行**
   - 快速开发功能时，用 AI 辅助写出基础代码
   - 功能完成后，集中时间做安全审查（不打断开发节奏）

2. **自动化安全扫描降低成本**
   
   - CI 集成 Gitleaks 后，每次 push 自动扫描
- 早期发现问题比后期修复成本低 10 倍
   
4. **建议的安全工作流程**
   
   开发功能 → AI 辅助审查 → 人工确认关键漏洞 → CI 自动扫描 → 安全检查清单 → 合并代码


